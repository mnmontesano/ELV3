# Render Blueprint specification
# Deploy to Render with one click

services:
  - type: web
    name: dob-violation-tracker
    env: node
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
